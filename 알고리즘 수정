#1. 반응물을 스페이스바로 구분되게 입력받아 각각 list 반응물, 생성물의 원소로 넣는다.

#2. list 반응물의 각 원소별로 숫자 앞에서 원소를 나눈다. 
  (이를 위해 원소의 개수가 1인 화합물은 원소뒤에 1을 입력받아 input한다. (예 : CO2->C1O2))

#3. 생성물의 입력받은 분자 순서대로 알파벳순으로 계수(미지수)를 지정해준다.

#4. 각 원소에 원소가 포함되어 있는 분자의 계수를 곱해준다.

#5. 반응물과 생성물의 list를 하나씩 만든다.

#6.  각 원소별로 list를 각각 만들고, 새로운 list에 append 한다. 이때, 계수가 뒤로 가도록 하고, (예 : CO2 -> C: 1a, O: 2a) 각 list들을 반응물과 생성물로 나누어 각각이 반응물list, 생성물list에 들어가도록 한다. 


#7. 반응물에서도 같은 방식을 적용하는데, list에 append 할때 -를 붙여준다.

#8. 반응물과 생성물의 원소들이 알파벳순으로 정렬된 list에 원소로 있는 상태에서, 생성물list의 n번째 list에 있는 원소들을 반응물 list의 n번째 list에 append 시킨다.

#9.  연립방정식 코드에 반응물list를 순서대로 넣고, 상수항은 전부 0으로 처리한다. 
